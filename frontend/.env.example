# The API Gateway HTTP API invoke URL (infra/envs/demo's `api_endpoint`
# output), no trailing slash. Baked in at build time, like PlayIT-nextjs's
# Vercel env vars -- changing this requires a rebuild, not just a redeploy.
VITE_API_BASE_URL=https://xxxxxxxxxx.execute-api.us-east-1.amazonaws.com
